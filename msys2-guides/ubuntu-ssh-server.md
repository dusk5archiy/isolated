# Ubuntu SSH Server

Run SSH Server

```bash
sudo systemctl start sshd
```

Connect to an SSH Server

```bash
ssh "<server-ip-address>"
```

(type username, password of an account in the SSH server...)

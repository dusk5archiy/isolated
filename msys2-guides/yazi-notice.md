# Yazi Notice

```bash
y     # use the command named "y" to open yazi
```
```
```
```
```

In Yazi:

- To open a new yazi tab, press `t`.
- To change directory, press `g<space>` then type a Windows path (`C:/users` for example).
